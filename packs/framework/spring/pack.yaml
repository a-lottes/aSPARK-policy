id: spring
category: framework
kind: baseline                # an opinionated default, meant to be overridden per org — never `final`
version: 1
maps_to_lens: null            # binds to the Developer via rules.code.spring; no existing lens covers framework conventions

summary: >
  Spring Framework conventions — layering, dependency injection, and
  configuration/secrets handling — as a starting scaffold. This is a
  baseline, not a mandate: override its specifics in your own policy.yaml.

references:
  - https://docs.spring.io/spring-framework/reference/
