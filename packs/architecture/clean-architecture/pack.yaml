id: clean-architecture
category: architecture
kind: baseline                # an opinionated default, meant to be overridden per org — never `final`
version: 1
maps_to_lens: null            # no `architecture` lens exists in aSPARK Core yet — applies as a constitution constraint

summary: >
  Clean Architecture conventions — dependency direction, layering, and
  architecture-decision-record discipline — as a starting scaffold. No
  aSPARK Core `architecture` lens exists yet, so this pack's rules apply as
  plain constitution constraints on the Engineering Manager and Reviewer
  until one does.

references:
  - https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html
