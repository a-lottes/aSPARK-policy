"""aspark-policy — Enterprise Policy-as-Code for the aSPARK product family.

This package is an empty scaffold. The `foundation` increment deliberately
ships no policy-parsing, schema, or CLI logic (spec §6, AC-6.5); later
increments add the format schema and the `aspark-policy validate` CLI here.
"""

__version__ = "0.0.1"
